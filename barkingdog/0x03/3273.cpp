#include <bits/stdc++.h>
using namespace std;
int arr[1000002];

int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  int n, m;

  cin >> n;
  for (int i = 0; i < n; i++)
  {
    if (i < n / 2)
    {
    }
  }
  cin >> m;
}
