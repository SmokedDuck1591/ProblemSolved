#include <bits/stdc++.h>
using namespace std;
char board[1002][1002];
int n, m;

int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> n >> m;
  for (int i = 0; i < n; i++)
    for (int j = 0; j < m; j++)
      cin >> board[i][j];
  
  
}