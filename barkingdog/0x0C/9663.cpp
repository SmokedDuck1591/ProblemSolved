#include <bits/stdc++.h>
using namespace std;
int n, ans;
int board[16][16];
void func(int k)
{
  if (k == n)
  {
    ans++;
    return;
  }
}

int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> n;
}