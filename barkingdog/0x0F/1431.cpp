#include <bits/stdc++.h>
using namespace std;
string str[52];
int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  int n;
  cin >> n;

  for (int i = 0; i < n; i++)
  {
    cin >> str[i];
    
  }
}