#include <bits/stdc++.h>
using namespace std;
int day[17];
int cost[17];ㅋ
int dp[17];
int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  int n;
  cin >> n;

  for (int i = 1; i <= n; i++)
    cin >> day[i] >> cost[i];

  for (int i =)
}