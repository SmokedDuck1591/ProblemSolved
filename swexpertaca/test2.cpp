#include <bits/stdc++.h>
using namespace std;

int main(void)
{
  ios::sync_with_stdio(0), cin.tie(0);
  string n, tmp;
  int x, y;
  int arr[1000002];
  cin >> n >> x >> y;

  for (int i = 0; i < n.length(); i++)
  {
    if (n[i] < x)
    {
      for (int j = 0; j < n.length(); j++)
      {
      }
    }
  }
}